# Production environment variables for Railway deployment

# Node environment
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# PostgreSQL Database (Railway will provide these values)
# These will be set as Railway environment variables
DATABASE_URL=${DATABASE_URL}

# Redis for queues and caching (Railway will provide these values)
# These will be set as Railway environment variables
REDIS_URL=${REDIS_URL}

# Optional: Manual Redis config if using external Redis
# REDIS_HOST=${REDIS_HOST}
# REDIS_PORT=${REDIS_PORT}
# REDIS_PASSWORD=${REDIS_PASSWORD}

# Queue Configuration (uses Redis)
QUEUE_REDIS_URL=${REDIS_URL}

# Optional: Manual database config if needed
# POSTGRES_HOST=${PGHOST}
# POSTGRES_PORT=${PGPORT}
# POSTGRES_USER=${PGUSER}
# POSTGRES_PASSWORD=${PGPASSWORD}
# POSTGRES_DB=${PGDATABASE}

# Logging
LOG_LEVEL=info

# Security
# Add your production secrets here as Railway environment variables
# JWT_SECRET=${JWT_SECRET}
# API_KEY=${API_KEY}

# AFIP Configuration (Argentina tax authority)
# AFIP_CERT_PATH=/app/certs/cert.crt
# AFIP_KEY_PATH=/app/certs/private.key
# AFIP_ENVIRONMENT=production

# WhatsApp Business API
# WHATSAPP_API_TOKEN=${WHATSAPP_API_TOKEN}
# WHATSAPP_PHONE_NUMBER_ID=${WHATSAPP_PHONE_NUMBER_ID}

# Email/SMTP Configuration
# SMTP_HOST=${SMTP_HOST}
# SMTP_PORT=${SMTP_PORT}
# SMTP_SECURE=${SMTP_SECURE}
# SMTP_USER=${SMTP_USER}
# SMTP_PASS=${SMTP_PASS}

# File Storage (if using cloud storage)
# STORAGE_TYPE=s3
# AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
# AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
# AWS_REGION=${AWS_REGION}
# AWS_S3_BUCKET=${AWS_S3_BUCKET}